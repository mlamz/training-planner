define(['jquery', 'underscore', 'backbone', 'router'], 
	function($, _, Backbone, router) {
    	var Workout = Backbone.Model.extend({
    		initialize: function(){
    		}
    	});

    	return Workout;
	}
);